import { Server } from "socket.io";
import express from "express";
import http from 'http';

const app = express(); // creates express app, what is this actually doing behind the scenes? 
console.log("test");